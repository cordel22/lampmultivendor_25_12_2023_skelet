// product_actions.js

//  NOT USED SO FAR!!!

function addToCart(productId) {
    // Implement AJAX request to add the product to the cart
    // You can use JavaScript fetch API or any other library like jQuery.ajax
    console.log(`Product added to cart: ${productId}`);
}

function modifyProduct(productId) {
    // Implement redirection to the vendor's index.php for product modification
    // You can use window.location.href or other methods
    console.log(`Redirecting to modify product: ${productId}`);
}

function removeProduct(productId) {
    // Implement AJAX request or redirection to remove the product
    // You can use JavaScript fetch API or window.location.href
    console.log(`Removing product: ${productId}`);
}